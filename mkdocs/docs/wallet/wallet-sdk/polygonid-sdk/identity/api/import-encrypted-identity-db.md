# To Import Identity Database: `exportEncryptedIdentityDb`

<!-- Add explanation for this api -->

 Future<void> importEncryptedIdentityDb({
    required String did,
    required String privateKey,
    required String encryptedDb,
  })